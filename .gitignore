# Hermes の実データ（記憶・SQLite・セッション・鍵）。絶対に git 管理しない。
/data/

# 秘密
.env
*.key

# ホスト固有設定（パスのみ・秘密は data/.env）。雛形 host.env.example は追跡する。
/host.env

# バックアップ成果物
/backups/
*.tar.gz
*.tar.zst

# OS / エディタ
.DS_Store
*.swp
